services:
  - type: web
    name: freshguard-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd api && python app.py
    envVars:
      - key: FLASK_ENV
        value: production